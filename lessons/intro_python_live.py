# coding: utf-8
this_is_a_variable = "variable"
this_is_a_variable
"What kind of data can a variable hold?"
variable = 10
variable = "piece of text"
variable = 1.23453
variable = [1,2,3,4]
type(variable)
type(variable)
variable = "pieces of text in python that can hold data"
integers = "whole numbers"
floats = "decimal numbers like 1.234"
integers = "whole numbers like 1 or 2 ..."
strings = "literally pieces of text with quotes or double quotes around them"
lists = "collections of values like [1,2,3,4] or ['text', 'another text', 1]"
print("How do I access a bunch of values inside of a list?")
task_list = ["teach python", "vibe code some apps", "go to the beach"]
print("What a heck is a loop Lucas?")
for t in task_list:
    print(t)
for_loops = "when you loop over a collection of elements like: for t in task_list:...."
variable
integers
floats
strings
lists
for_loops
print("Here is the picture: you have a folder and you want to do something with the file names inside of it")
folder_path = "./"
packages = "things we can import inside the Python environment (script..) to use extra functionalities"
packages = "things we can import inside the Python environment (script..) to use extra functionalities, 'import package'"
import os
os.listdir(folder_path)
type(os.listdir(folder_path))
lists
files_inside_the_folder = os.listdir(folder_path)
files_inside_the_folder
for_loops
task = "you have a folder and you want to do something with the file names inside of it"
for file in files_inside_the_folder:
    print(file)
file
file = "extras"
file
type(file)
file.capitalize()
file
files_inside_the_folder
file.lower()
file.uppercase()
file.upper()
file.endswith()
file.endswith()
file.endswith(".md")
file.endswith(".py")
file.endswith(" ")
file.endswith("")
type(file.endswith(""))
bool_value = "Logical data type thing it can be either True or False, use for decisions, like if this is True..."
file
file.endswith("alkndlasndas")
bool_value
files_inside_the_folder
file_name_for_setup = files_inside_the_folder[2]
file_name_for_setup
file_name_for_setup.endswith(".md")
task
task = "Inside a folder get all the files that end with markdown"
for file in files_inside_the_folder:
    print(file.endswith(".md"))
QUESTION = "How do we check IF a condition is TRUE to get ONLY THE MARKDOWN FILES?"
QUESTION = "How do we check IF a condition is TRUE to get ONLY THE MARKDOWN FILES?"
QUESTION
file.endswith(".md") eq true
if(file.endswith())
if(file.endswith()):
    print("hi")
if(file.endswith("")):
    print("hi")
file
if file.endswith(".md):
    print(file)
if file.endswith(".md"):
    print(file)
file
file_name_for_setup
if file_name_for_setup.endswith(".md"):
    print(file, " is a markdown!")
if file_name_for_setup.endswith(".md"):
    print(file_name_for_setup, " is a markdown!")
if_statements = "use the 'if' keyword to check if statements are true like 'if file.endswith(...):....'"
task
files_inside_the_folder
task
for file in files_inside_the_folder:
    if file.endswith(".md"):
        print("Markdown file found!", file)
task